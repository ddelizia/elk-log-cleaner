{
  "name": "elk-log-cleaner",
  "version": "1.0.0",
  "description": "Logstash produces indexes in elasticsearch for each day. This script gets all the indexes and deletes the ones older than a date",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.3.0",
    "moment": "^2.20.1",
    "nconf": "^0.10.0",
    "request": "^2.83.0"
  }
}
